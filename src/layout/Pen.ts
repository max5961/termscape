export type PenOpts = {
    /**
     * Does the pen inherit the last pens position? Or does it reset to the
     * corner position?
     *
     * @default false
     * */
    linked?: boolean;
};

export class Pen {
    constructor(context, opts: PenOpts = {}) {
        //
    }
}
